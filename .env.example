# React Native App Environment Variables
# Copy this file to .env and update with your actual values

# Gemini API Key for AI image generation and analysis
GEMINI_API_KEY=your_gemini_api_key_here

# Network Configuration for mobile app connectivity
# Option 1: For Expo tunnel mode (recommended if LAN doesn't work)
EXPO_PUBLIC_SERVER_URL=https://your-ngrok-url.ngrok-free.app

# Option 2: For Expo LAN mode (if on same WiFi network)
# Find your IP with 'ipconfig' (Windows) or 'ifconfig' (Mac/Linux)
EXPO_PUBLIC_LAPTOP_IP=your.laptop.ip.address
EXPO_PUBLIC_SERVER_PORT=3001